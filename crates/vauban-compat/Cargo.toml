[package]
name = "vauban-compat"
version.workspace = true
edition.workspace = true
publish.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true

[dependencies]
vauban-session.workspace = true
vauban-sysfn.workspace = true
vauban-types.workspace = true
vauban-errors.workspace = true

# `tests/sp_datatype_info.rs` builds a `Session` over an in-memory `Storage` and a
# `ResultSink` that keeps what it receives: `storage` for the trait, `tds` for `ColumnMeta`,
# `EnvChange` and `Rpc`, which `session` takes in its interfaces without re-exporting them.
[dev-dependencies]
vauban-storage.workspace = true
vauban-tds.workspace = true

[lints]
workspace = true
